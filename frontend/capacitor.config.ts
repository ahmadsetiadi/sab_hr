import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'as.hr.sinar',
  appName: 'sinar',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
